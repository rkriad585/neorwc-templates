# Plan: System Package Docs
**Goal:** Document how this software integrates with the OS.

**Required Files:**
1.  `docs/install/debian.md`: Instructions for `.deb` or `apt` installation.
2.  `docs/config/files.md`: Explanation of configuration files found in `/etc/`.
3.  `docs/admin/troubleshooting.md`:
    - How to check logs (`journalctl`).
    - How to restart the service (`systemctl restart`).
