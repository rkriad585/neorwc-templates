# Plan: README Overhaul

**Goal:** Create a high-quality `README.md` that makes developers want to install Neorwc immediately.

**Required Files:**
1.  `README.md` (Root directory)

**Content Requirements:**
- **Title & Badges:** Neorwc + License/Node Version badges.
- **The "Why":** Explain that this tool uses local LLMs (Ollama) to write docs, so code never leaves the machine.
- **Key Features List:**
    - Local LLM Support (Llama3, Mistral, etc).
    - Dynamic Context Window detection.
    - Global Plans & Skills system (`~/.neorwc`).
    - GitHub Template Store integration (`--install`).
- **Quick Start:**
    - `npm install -g neorwc`
    - `neorwc --init`
    - `neorwc`
- **Configuration:** Brief explanation of `neorwc.md`.
