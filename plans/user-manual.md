# Plan: Complete User Manual

**Goal:** Create a `docs/` folder with detailed guides on every feature.

**Required Files:**
1.  `docs/usage/installation.md`: Global installation vs local usage.
2.  `docs/usage/commands.md`: detailed table of all flags (`-i`, `-t`, `-c`, etc).
3.  `docs/features/templates.md`: How to use the `--install` and `--templates` feature.
4.  `docs/features/customization.md`: How to create custom Skills and Plans in `~/.neorwc`.

**Context:**
- Analyze `neorwc.js` to get the list of flags.
- Analyze `src/core/templates.js` to understand how GitHub fetching works.
